#version 430

layout(local_size_x = 16, local_size_y = 16) in;
layout(r32f, binding = 0) uniform image2D densityMap;

void main() {
    ivec2 pixelCoords = ivec2(gl_GlobalInvocationID.xy);
    imageStore(densityMap, pixelCoords, vec4(0.0));
}